<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.suli.myutils">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <uses-sdk tools:overrideLibrary="com.example.common"/>

    <application
        android:name=".GlobalContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="7b92a519e9707fba20135cae65a7d4279b99fb50"/>


        <activity android:name=".fragment.library.TestVolleyActivity"
            />

        <activity android:name=".fragment.library.TestRetrofitActivity"
            />

        <activity android:name=".fragment.library.TestRxJavaActivity"
            />

        <activity android:name=".fragment.principle.ViewDrawActivity"
            />

        <activity android:name=".fragment.performance.NextActivity"
            />

    </application>

</manifest>
